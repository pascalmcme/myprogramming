ageOfPatient = 'five'  
age = 5 # Note Variables start with character

print(type(age))  #apply type fucntion to find the type of variable age, and display it with print.


print("you are " +str(age)) # apply string formula to the variable age - so that we can print it with the other string. "can only concatenate str"

print("Type of age is "+ str(type(age)))  # first convert the answer to type to a string. 

age = 5
nextYear = age + 1  # cannot add to strings
print("next year you will be "+ str(nextYear))  #convert back to a string to print. 

# note "...{x} " format(x) is an alternative